# Optional local Vite settings; copy to .env.local.
# /api uses Vite's development proxy to localhost:8080.
VITE_API_BASE_URL=/api
# Railway: set an absolute HTTPS backend URL ending in /api in service variables.
# Never place DB_PASSWORD, JWT_SECRET or WABI_API_KEY in the frontend.
